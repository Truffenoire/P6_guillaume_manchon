Ajoutez un dossier "images" au dossier backend.
renommez le .envCopie en .env et remplissez les clefs avec vos valeur, la premiere clef etant la connection a mongodb.
lancez le frontend avec la commande "npm start" dans la console.
lancez le backend avec la commande "nodemon server".